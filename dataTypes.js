// string

let myName = "David";
let upperCaseMyName = myName.toUpperCase(); 

console.log(upperCaseMyName);  // output: DAVID

// Primitives are imutable, but in the above, the toUpperCase method is creating a new string and it is passing the name to the new variable upperCaseMyName.